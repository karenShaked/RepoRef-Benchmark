# Extended set (~1.5k instances)

A larger pool of CoRG instances, same format as the core `main` benchmark
(`instances.jsonl`, `gold.jsonl`, `metadata.csv`).

**Use it for scale** — extra training/analysis signal. **Report benchmark results on `main`.**


